# Copy to .env.local and restart `npm run dev`
# Frontend luôn gọi backend LOCAL (mặc định):
# VITE_API_BASE=/api

# Colab tracking → cấu hình ở .env GỐC repo (backend), KHÔNG đặt ở đây:
# ../.env  →  COLAB_TRACKING_URL=https://xxxx.ngrok-free.app

# (Cũ) Full pipeline trên Colab — dễ OOM, không khuyến nghị:
# VITE_API_BASE=https://xxxx.ngrok-free.app/api
