# Copy to .env.local and restart `npm run dev`
# Local backend (default — leave unset or use /api via Vite proxy):
# VITE_API_BASE=/api

# Colab GPU backend (paste URL from Colab notebook cell, include /api suffix):
# VITE_API_BASE=https://xxxx-xxxx.ngrok-free.app/api
